def encrypt_data(data: str, key: str) -> str:
    # Заглушка — в реальном проекте использовать Fernet или AES
    return data

def decrypt_data(encrypted_data: str, key: str) -> str:
    # Заглушка
    return encrypted_data